server:
  port: 8200

spring:
  application:
    name: service-goods
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        ephemeral: false
      config:
        server-addr:  localhost:8848
        file-extension: yaml
        group: goods